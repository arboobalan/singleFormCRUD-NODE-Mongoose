<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
  <title>Create & Update</title>
  <style>
    .colStyle {
      margin: 20px;
      padding: 10px;
      background-color: #f0f8ff;
      border: 2px solid #000;
      border-radius: 5px;
    }
  </style>
</head>

<body>
  <section>

    <form action="/myform" method="post">
      <div class="row">
        <div class="col-md-4"></div>
        <div class="col-md-4 colStyle">

          <h4 class="text-center text-primary">Create & Update</h4>

          <input type="hidden" name="id" value="<%= users ? users._id :'' %>">

          <div class="form-group">
            <label for="username">Username</label>
            <input type="text" name="username" value="<%= users ? users.username : '' %>" class="form-control">
          </div>

          <div class="form-group">
            <label for="email">Email</label>
            <input type="text" name="email" value="<%=   users ? users.email : '' %>" class="form-control">
          </div>

          <div class="form-group">
            <label for="gender">Select Gender</label>
            <select name="gender" id="gender" class="form-control">
              <option value="gender">Select</option>
              <option value="Male" <%= users && users.gender==='Male'?'selected':''%>>Male</option>
              <option value="Female" <%= users && users.gender==='Female'?'selected':'' %>>Female</option>
            </select>
          </div>

          <div class="form-group">
            <label for="mobile">Mobile</label>
            <input type="text" name="mobile" value="<%= users ? users.mobile : '' %>" class="form-control">
          </div>

          <div class="form-group">
            <button class="btn btn-sm btn-outline-success">Save Info</button>
          </div>

        </div>
        <div class="col-md-4"></div>
      </div>
    </form>
  </section>
</body>

</html>